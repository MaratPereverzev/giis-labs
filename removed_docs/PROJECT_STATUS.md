# GIIS Lab 1 - ГОТОВО

## Проект полностью переработан и готов

### Что было сделано:

1. **Упрощение стилистики**
   ✓ Убрали все эмодзи (🎨, ↔️, ←, → и т.д.)
   ✓ Убрали все анимации (slideDown, transitions и т.д.)
   ✓ Минимальный дизайн (простой серый цвет #f8f9fa)
   ✓ Без градиентов и сложных эффектов
   ✓ Без иконок

2. **Главное меню упрощено**
   ✓ Убрана большая надпись "GIIS Labs - Упрощённый Photoshop"
   ✓ Убрана фраза "Выберите лабораторную работу для начала"
   ✓ Остались только простые карточки

3. **Оставлена только Lab 1**
   ✓ Удалены Lab2-7 из конфигурации
   ✓ Кнопки навигации скрыты (будут видны при добавлении новых лаб)
   ✓ Счетчик показывает "1 / 1"

4. **Реализована Lab 1 - Построение отрезков**
   ✓ Три алгоритма растеризации линий:
     - DDA (Digital Differential Analyzer)
     - Брезенхем (целочисленный алгоритм)
     - Ву (с антиалиасингом/сглаживанием)
   ✓ Интерактивный canvas с сеткой пикселей
   ✓ Нажимаете на canvas - устанавливается точка
   ✓ Вторая точка - рисуется линия
   ✓ Видно координаты обеих точек
   ✓ Кнопка очистки
   ✓ Информация о текущем алгоритме

### Структура проекта:

```
src/
├── App.tsx                  - маршрутизация
├── components/
│   ├── Home.tsx            - упрощенное главное меню
│   └── LabLayout.tsx       - обёртка для лабораторной
├── labs/
│   ├── config.ts           - ТОЛЬКО LAB 1
│   └── lab1/
│       ├── Lab1.tsx        - компонент Lab 1
│       └── Lab1.css        - стили (без анимаций)
├── utils/
│   └── lineDrawing.ts      - 3 алгоритма рисования линий
├── styles/
│   ├── Home.css            - упрощенные стили меню
│   └── LabLayout.css       - упрощенные стили навигации
└── types/
    └── lab.ts              - TypeScript интерфейсы
```

### Запуск:

```bash
cd /home/marat/Documents/BSUIR/GIIS/giis-labs
npm install  # если еще не установлено
npm run dev
```

Откроется на http://localhost:5174

### Сборка проекта:

✓ Успешно компилируется
✓ Нет ошибок TypeScript
✓ Все модули преобразованы
✓ Size: 234.85 kB (gzip: 75.49 kB)

### Документация:

- LAB1_INFO.md - информация о Lab 1
- LAB1_MANUAL.md - инструкция по использованию
- READY.txt - краткая сводка

### Алгоритмы в utils/lineDrawing.ts:

```typescript
// DDA алгоритм
drawLineDDA(x0, y0, x1, y1): Point[]

// Алгоритм Брезенхема
drawLineBresenham(x0, y0, x1, y1): Point[]

// Алгоритм Ву с антиалиасингом
drawLineWu(x0, y0, x1, y1): Array<Point & { intensity: number }>
```

### Использование Lab 1:

1. На главной странице видна одна карточка "Лаб 1"
2. Нажимаете на карточку
3. В Lab 1:
   - Выбираете алгоритм (кнопки слева)
   - Нажимаете на canvas для начальной точки
   - Нажимаете еще раз для конечной точки
   - Автоматически рисуется линия выбранным алгоритмом

### Готово к использованию!

Проект полностью готов к разработке и использованию.
Можно запускать приложение и работать с Lab 1.

При добавлении новых лабораторных работ:
1. Создайте компонент LabN.tsx
2. Добавьте импорт в config.ts
3. Добавьте объект в LABS_CONFIG
4. Кнопки навигации автоматически появятся
