# 🎨 GIIS Labs - Структура проекта

## ✅ Что было сделано

Создана полностью настроенная структура React + TypeScript проекта для 7 лабораторных работ с:

### 🏗️ Архитектура
- **React Router** для навигации между лабораторными работами
- **TypeScript** для типобезопасности
- **Vite** для быстрой разработки и сборки
- Модульная структура для легкого добавления новых работ

### 📁 Структура файлов
```
src/
├── components/
│   ├── Home.tsx          # 🏠 Главное меню со всеми лабораторными
│   └── LabLayout.tsx     # 📋 Общая обёртка с навигацией
├── labs/
│   ├── config.ts         # ⚙️  Конфигурация всех лабораторных
│   ├── lab1/Lab1.tsx     # Шаблон каждой лабораторной
│   ├── lab2/Lab2.tsx
│   └── ... до lab7
├── styles/
│   ├── Home.css          # Стили главного меню
│   └── LabLayout.css     # Стили лаб-работ
├── types/
│   └── lab.ts            # TypeScript интерфейсы
├── App.tsx               # Основное приложение с маршрутизацией
├── App.css
├── index.css
└── main.tsx              # Entry point
```

### 🎯 Основные компоненты

1. **Home.tsx** - красивое главное меню с 7 кнопками для выбора лабораторной
2. **LabLayout.tsx** - навигационный компонент с:
   - Кнопкой "Главное меню"
   - Кнопками "Пред" / "След" для перемещения между лабораторными
   - Счётчиком текущей лабораторной (N/7)
3. **config.ts** - конфигурация всех лабораторных с описаниями

### 🎨 Дизайн
- Фиолетовый градиент для главного меню
- Адаптивный дизайн (мобильные устройства)
- Гладкие анимации и переходы
- Профессиональная типографика

## 🚀 Быстрый старт

```bash
# Установить зависимости
npm install

# Запустить в режиме разработки
npm run dev

# Собрать для продакшена
npm run build
```

Приложение откроется на [http://localhost:5173](http://localhost:5173)

## 📝 Как добавить свою лабораторную работу

### Вариант 1: Быстро (используя готовый шаблон)
Шаблоны уже готовы в `src/labs/lab1` до `src/labs/lab7`. Просто отредактируйте нужный файл и добавьте логику.

### Вариант 2: Создание нового компонента

1. **Создайте компонент**
   ```tsx
   // src/labs/labN/LabN.tsx
   export function LabN() {
     const [state, setState] = useState(null);
   
     return (
       <div className="lab-component">
         <h1>Лабораторная работа N</h1>
         <p>Описание задачи</p>
         
         {/* Ваш код здесь */}
         <canvas id="canvas"></canvas>
       </div>
     );
   }
   ```

2. **Обновите config.ts**
   ```tsx
   import { LabN } from './labN/LabN';
   
   export const LABS_CONFIG: Lab[] = [
     // ...
     {
       id: N,
       title: 'Название работы',
       description: 'Описание',
       path: 'labN',
       component: LabN,
     },
   ];
   ```

3. **Готово!** Навигация обновится автоматически

## 💡 Полезные советы

### Работа с Canvas (если нужна графика)
```tsx
useEffect(() => {
  const canvas = document.getElementById('canvas') as HTMLCanvasElement;
  const ctx = canvas.getContext('2d');
  // Ваш код
}, []);
```

### Загрузка изображений
```tsx
const [image, setImage] = useState<HTMLImageElement | null>(null);

const handleImageUpload = (e: React.ChangeEvent<HTMLInputElement>) => {
  const file = e.target.files?.[0];
  if (file) {
    const img = new Image();
    img.src = URL.createObjectURL(file);
    img.onload = () => setImage(img);
  }
};
```

### Использование массивов пикселей (ImageData)
```tsx
const canvas = canvasRef.current;
const ctx = canvas.getContext('2d');
const imageData = ctx.getImageData(0, 0, canvas.width, canvas.height);
const data = imageData.data;

// Обработка пикселей
for (let i = 0; i < data.length; i += 4) {
  // data[i]   = R
  // data[i+1] = G
  // data[i+2] = B
  // data[i+3] = A (alpha)
}

ctx.putImageData(imageData, 0, 0);
```

## 📚 Рекомендуемые лабораторные работы

1. **Lab 1** - Конвертер цветовых пространств (RGB → HSV → CMYK)
2. **Lab 2** - Трансформация изображений (масштаб, поворот, перспектива)
3. **Lab 3** - Фильтры (размытие, резкость, собель, лаплас)
4. **Lab 4** - Морфология (эрозия, расширение, открытие, закрытие)
5. **Lab 5** - Сегментация (пороговая обработка, водораздел)
6. **Lab 6** - Текстура (анализ, синтез, классификация)
7. **Lab 7** - Комплексное приложение (комбинация всех методов)

## 🔧 Технологический стек

- **React 18** - UI библиотека
- **React Router v7** - навигация
- **TypeScript** - типизация
- **Vite 7** - сборщик
- **CSS 3** - стили (Flexbox, Grid, Градиенты, Анимации)

## 🐛 Отладка

Если возникли проблемы:
1. Проверьте консоль браузера (F12)
2. Удалите `node_modules` и выполните `npm install`
3. Очистите кэш Vite: `npm run dev -- --force`

## 📖 Дополнительные ресурсы

- [React документация](https://react.dev)
- [TypeScript документация](https://www.typescriptlang.org)
- [Vite документация](https://vitejs.dev)
- [React Router документация](https://reactrouter.com)

---

**Проект готов к разработке! 🚀**
