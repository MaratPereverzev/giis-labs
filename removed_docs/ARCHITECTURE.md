# 📚 Структура проекта GIIS Labs

Это модульный проект для 7 лабораторных работ, имитирующий упрощённое приложение Photoshop.

## 🏗️ Структура папок

```
src/
├── components/           # Переиспользуемые компоненты
│   ├── Home.tsx         # Главное меню
│   └── LabLayout.tsx    # Общая обёртка для всех лабораторных
├── labs/                # Лабораторные работы
│   ├── config.ts        # Конфигурация всех лабораторных
│   ├── lab1/
│   ├── lab2/
│   ├── ...
│   └── lab7/
├── styles/              # Глобальные стили
│   ├── Home.css
│   └── LabLayout.css
├── types/               # TypeScript типы
│   └── lab.ts
└── App.tsx             # Маршрутизация приложения
```

## 🚀 Как добавить новую лабораторную работу

### Шаг 1: Создать компонент
Создайте файл `/src/labs/labN/LabN.tsx`:

```tsx
export function LabN() {
  return (
    <div className="lab-component">
      <h1>Лабораторная работа N</h1>
      <p>Описание вашей задачи</p>
      
      {/* Ваш код здесь */}
      <div className="your-content">
        {/* Реализация */}
      </div>
    </div>
  );
}
```

### Шаг 2: Обновить конфигурацию
Добавьте импорт в `/src/labs/config.ts`:

```tsx
import { LabN } from './labN/LabN';

export const LABS_CONFIG: Lab[] = [
  // ... другие лабораторные
  {
    id: N,
    title: 'Название вашей работы',
    description: 'Короткое описание',
    path: 'labN',
    component: LabN,
  },
];
```

## 🎨 Использование стилей

Все стили уже подготовлены. Вы можете использовать:

- `.lab-component` - контейнер для содержимого
- `.placeholder` - заполнитель для демонстрации
- Встроенные стили из `Home.css` и `LabLayout.css`

Пример с собственными стилями:

```tsx
export function LabN() {
  return (
    <div className="lab-component">
      <h1>Лабораторная работа N</h1>
      <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '20px' }}>
        {/* Содержимое */}
      </div>
    </div>
  );
}
```

## 📦 Навигация

Приложение автоматически поддерживает навигацию между лабораторными:
- 🏠 Кнопка "Главное меню" - вернуться в главное меню
- ⬅️ Кнопка "Пред" - перейти на предыдущую работу
- ➡️ Кнопка "След" - перейти на следующую работу
- 📊 Счётчик "N / 7" - показывает текущую работу

## 🛠️ Установка зависимостей

```bash
npm install
```

## 🏃 Запуск проекта

```bash
npm run dev
```

Приложение откроется на `http://localhost:5173`

## 📝 Инструкции к Vite + React + TypeScript

- **Горячая перезагрузка (HMR)** - изменения отражаются в реальном времени
- **TypeScript** - для типобезопасности кода
- **CSS Modules** опционально - можно использовать обычные CSS файлы

## 🎯 Функции каждой лаборатории

1. **Lab 1** - Основы цветового пространства (RGB, HSV, CMYK)
2. **Lab 2** - Геометрические трансформации (масштаб, поворот)
3. **Lab 3** - Фильтры и свёртка (размытие, резкость)
4. **Lab 4** - Морфологические операции (эрозия, расширение)
5. **Lab 5** - Сегментация изображения (пороговая обработка)
6. **Lab 6** - Текстурный анализ
7. **Lab 7** - Комплексная обработка

## 💡 Полезные советы

- Используйте `useState` для управления состоянием
- Создавайте отдельные компоненты для сложной логики
- Используйте CSS Grid/Flexbox для компоновки
- Тестируйте на мобильных устройствах (проект адаптивный)
